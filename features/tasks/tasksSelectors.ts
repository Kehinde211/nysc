// import { RootState } from "@/lib/store"

// export const selectAllTasks = (state: RootState) => {
//     state.tasks.entities;
// }

// export const selectPendingTasks = (state: RootState) => {
//     state.tasks.entities(t => t.status === "pending")
// }

// export const selectUrgentTasks = (state: RootState) => {
//     state.tasks.entities(t => t.status === "urgent")
// }

// export const selectDueTasks = (state: RootState) => {
//     state.tasks.entities(t => t.status === "due")
// }
